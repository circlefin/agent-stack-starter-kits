{
  "name": "@agent-stack-ecosystem-kits/kit-openai-agents",
  "version": "0.0.0",
  "private": true,
  "description": "Autonomous Payment Agent example using the OpenAI Agents SDK + Circle Agent Stack.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "demo": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agent-stack-ecosystem-kits/agent-cli": "workspace:*",
    "@agent-stack-ecosystem-kits/circle-tools": "workspace:*",
    "@openai/agents": "^0.11.0",
    "dotenv": "^17.4.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
